/**
 * @file scene_registration.h
 * @brief User-defined scene and component registration for the SceneContent library.
 */
#pragma once

class SceneManager;

void RegisterUserScenes(SceneManager& manager);
void RegisterUserComponents();
